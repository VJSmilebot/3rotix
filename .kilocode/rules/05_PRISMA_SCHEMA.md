Prisma rules:

- Prisma schema mirrors the live Supabase database
- Prisma migrations are NOT the deployment mechanism
- Schema changes are applied via Supabase SQL
- Prisma schema is updated to stay in sync
